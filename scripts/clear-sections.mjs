import { createClient } from '@supabase/supabase-js'; import * as dotenv from 'dotenv'; dotenv.config({ path: '.env.local' }); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); (async () => { console.log('Clearing sections...'); const { error } = await supabase.from('section').delete().not('id', 'is', null); if (error) console.error('Error:', error); else console.log('Done\!'); process.exit(0); })();
